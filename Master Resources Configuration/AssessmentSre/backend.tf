# Fill these based on outputs from bootstrap/tfstate.
/* terraform {
  backend "azurerm" {
    resource_group_name  = "sre-db-rg" # <- replace
    storage_account_name = "sredbstorage"   # <- replace
    container_name       = "srestatefile"
    key                  = "assess.terraform.tfstate"
  }
} */
