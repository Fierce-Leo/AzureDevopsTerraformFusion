# Fill these based on outputs from bootstrap/tfstate.
/*terraform {
  backend "azurerm" {
    resource_group_name  = "Chennai-DC-BCP" # <- replace
    storage_account_name = "bcpdbstorage"   # <- replace
    container_name       = "bcpstatefile"
    key                  = "assess.terraform.tfstate"
  }
} */
